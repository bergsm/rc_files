# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# colors
set_from_resource $bg-color wal.color0 #000000
set_from_resource $font-color wal.color6 #000000
set_from_resource $fg-color wal.color3 #ff0000
#set_from_resource $font-color wal.color1 #000000
#set_from_resource $fg-color wal.color1 #ff0000

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
#exec --no-startup-id xss-lock --transfer-sleep-lock -- multilockscreen -l blur --nofork
#exec --no-startup-id xss-lock /usr/local/bin/idleSuspend --nofork
exec --no-startup-id idleSuspend

#opactiy messes up scrolling
#exec --no-startup-id compton --config /home/bergsm/.config/compton/compton.conf
exec --no-startup-id picom

#exec i3-sensible-terminal

#exec --no-startup-id bash /home/bergsm/pics/startup2/startup.sh

exec --no-startup-id feh /home/bergsm/pics/wallpapers/wallpaper --bg-fill
exec --no-startup-id wal -R

# geoloc for redshift
exec --no-startup-id /usr/lib/geoclue-2.0/demos/agent

exec --no-startup-id /usr/bin/redshift

#auto detect monitor
#exec --no-startup-id /usr/bin/x-on-resize

#TODO does this work? copy paste from and terminal
exec --no-startup-id /usr/bin/autocutsel -s PRIMARY -fork
exec --no-startup-id /usr/bin/autocutsel -fork

#exec --no-startup-id xscreensaver

for_window [class="^.*"] border pixel 2
#default_border pixel 4
#client.focused    #DED687 #DED687 #DED687
client.focused    $font-color $font-color $font-color

for_window [class="feh"] floating enable

gaps inner 10
#gaps outer 10
smart_borders on

bindsym Control+Delete exec --no-startup-id multilockscreen -l blur > /dev/null 2>&1
#bindsym Control+Delete exec --no-startup-id multilockscreen -l > /dev/null 2>&1
#bindsym $mod+Escape exec --no-startup-id multilockscreen -l > /dev/null 2>&1
#TODO
bindsym $mod+Shift+Escape exec "i3-msg exit; sudo chvt 1"
# Screenshots
bindsym Control+Shift+4 exec "maim -s ~/pics/screenshots/$(date '+%b%d%Y_%H%M').png"
bindsym Control+Shift+3 exec "maim ~/pics/screenshots/$(date '+%b%d%Y_%H%M').png"
bindsym $mod+Control+Shift+4 exec "maim -s | xclip -selection clipboard -t image/png"
bindsym $mod+Control+Shift+3 exec "maim | xclip -selection clipboard -t image/png"


# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
#exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+i exec firefox

#bindsym $mod+Shift+` exec lock

# kill focused window
bindsym $mod+q kill
#bindsym Control+Alt + Tab kill
#bindsym Control+w kill
#bindsym Control+q kill

# start dmenu (a program launcher)
#bindsym $mod+space exec dmenu_run
#bindsym Control+space exec dmenu_run -i -fn "xft:Iosevka-13:style=medium" -nb "$fg-color" -nf "$bg-color" -sb "$bg-color" -sf "$fg-color"
#TODO adjust so it dynamically uses system font
bindsym Control+space exec dmenu_run -i -h 39 -fn "Iosevka-13:style=medium" -nb "$fg-color" -nf "$bg-color" -sb "$bg-color" -sf "$fg-color"
#bindsym Control+space exec dmenu_run -i -dim 0.6 -h 40 -fn "Iosevka-13:style=medium" -nb "$fg-color" -nf "$bg-color" -sb "$bg-color" -sf "$fg-color"
#bindsym Control+space exec rofi -show run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
#TODO cycle
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
#bindsym Control+Left focus left
#bindsym Control+Down focus down
#bindsym Control+Up focus up
#bindsym Control+Right focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right
bindsym $mod+Shift+Tab focus left
bindsym $mod+Tab focus right
#bindsym Control+Shift+Tab focus left
#bindsym Control+Tab focus right

#move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right
#bindsym Shift+Control+Left move left
#bindsym Shift+Control+Down move down
#bindsym Shift+Control+Up move up
#bindsym Shift+Control+Right move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
#bindsym $mod+Shift+s split h

# split in vertical orientation
#bindsym $mod+s split v
bindsym $mod+s split toggle

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

#For minimizing programs
bindsym $mod+m move scratchpad
bindsym $mod+Shift+m scratchpad show
#bindsym $mod+Up fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
#bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+f floating toggle

# change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle

#screenshot
bindsym Print exec flameshot gui

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# set work spaces to displays
workspace 1 output HDMI-A-0
workspace 2 output eDP

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

#Move workspace to monitor
bindsym $mod+Shift+Control+1 move workspace to output down
bindsym $mod+Shift+Control+2 move workspace to output up
bindsym $mod+Shift+Control+Down move workspace to output down
bindsym $mod+Shift+Control+Up move workspace to output up
bindsym $mod+Shift+Control+j move workspace to output down
bindsym $mod+Shift+Control+k move workspace to output up

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

#focus_follows_mouse no

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize grow height 2 px or 2 ppt
        bindsym k resize shrink height 2 px or 2 ppt
        #bindsym j resize grow width 2 px or 2 ppt
        #bindsym k resize shrink width 2 px or 2 ppt
        bindsym h resize grow width 2 px or 2 ppt
        bindsym l resize shrink width 2 px or 2 ppt

        # same bindings, but for the arrow keys
        bindsym Up resize shrink height 2 px or 2 ppt
        bindsym Down resize grow height 2 px or 2 ppt
        #bindsym Up resize shrink width 2 px or 2 ppt
        #bindsym Down resize grow width 2 px or 2 ppt
        bindsym Right resize shrink width 2 px or 2 ppt
        bindsym Left resize grow width 2 px or 2 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)

bar {
    #disable system tray
    tray_output none
    #TODO make dynamic with Xresources
    font pango:Iosevka Medium
    #font Iosveka-13 40px
    colors {
        #background $bg-color
        #background #000000BB
        background #000000BB
        #background #00000000
        #separator #757575
        separator $bg-color
        statusline $font-color
        #statusline $fg-color
        focused_workspace    $bg-color    $fg-color    $bg-color
        }
        #TODO font color
        #font xft:Isoveka-12 14px
        #status_command i3status
        i3bar_command i3bar --transparency
        #i3bar_command i3bar
        status_command i3blocks
        position top
        #mode hide
}
